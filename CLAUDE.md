# Pokenese — Project Guide

## What Is This

Pokenese is a Wordle-style daily game that teaches Mandarin Chinese through Pokémon names. Players see a Pokémon's Chinese name and guess the English name, receiving hints on wrong guesses.

## Key Documents

- `README.md` — High-level goals and feature overview (source of truth for intent)
- `SPEC.md` — Full technical and functional specification (architecture, APIs, data models, game logic)
- `VISUAL_DESIGN_SPEC.md` — Complete UI/UX specification (design tokens, screen layouts, components)

## Planned Stack

- **Frontend:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, Framer Motion, Zustand, TanStack Query
- **Backend:** Python FastAPI, PostgreSQL, SQLAlchemy async, JWT auth
- **Data:** TypeScript constants in `frontend/src/data/pokemon.ts` (generated by scraping script)

## Project Structure

```
Pokenese/
├── frontend/          # Next.js app (IMPLEMENTED)
├── backend/           # FastAPI app (IMPLEMENTED)
│   ├── app/
│   │   ├── main.py              # FastAPI app entry point
│   │   ├── config.py            # pydantic-settings config
│   │   ├── database.py          # async SQLAlchemy engine
│   │   ├── dependencies.py      # get_db, get_current_user, get_optional_user
│   │   ├── pokemon_data.py      # loads data/pokemon.json (or sample)
│   │   ├── models/              # SQLAlchemy ORM models
│   │   ├── schemas/             # Pydantic v2 request/response models
│   │   ├── routers/             # FastAPI route handlers
│   │   └── services/            # Business logic (auth, daily, challenge, scoring)
│   ├── scripts/
│   │   ├── scrape_pokemon.py    # Scrapes PokéAPI + Bulbapedia
│   │   ├── seed_daily.py        # Seeds daily_challenges table for 1 year
│   │   ├── migrate.py           # Runs Alembic migrations
│   │   └── generate_pokemon_json.py  # Converts pokemon.ts → pokemon.json
│   ├── alembic/                 # DB migrations (001_initial.py creates all tables)
│   ├── data/
│   │   └── pokemon_sample.json  # 20-Pokemon sample for dev/testing
│   ├── requirements.txt
│   ├── .env.example
│   ├── Dockerfile
│   └── docker-compose.yml
├── SPEC.md            # Technical specification
├── VISUAL_DESIGN_SPEC.md  # Design specification
├── README.md          # Project overview
└── CLAUDE.md          # This file
```

## Implementation Status

Backend: COMPLETE. Frontend: COMPLETE (build passes).

### Frontend Setup Steps
1. `cd frontend`
2. `cp .env.local.example .env.local`
3. `npm install`
4. `npm run dev` — starts at http://localhost:3000

### Backend Setup Steps
1. Copy `.env.example` to `.env` and set `SECRET_KEY`
2. Start Postgres: `docker-compose up db -d`
3. Run migrations: `python scripts/migrate.py`
4. Seed daily challenges: `python scripts/seed_daily.py`
5. (Optional) Scrape Pokemon data: `python scripts/scrape_pokemon.py`
6. Start server: `uvicorn app.main:app --reload`

## Notes

- Mobile-first design
- Anonymous users supported via localStorage; accounts optional
- Pokémon data scraped and stored as TypeScript constants (not fetched at runtime from external APIs)
- Daily challenges pre-seeded in DB; same 3 Pokémon for all users worldwide each day
- Challenge mode resets on complete failure (score 0 on a challenge)
